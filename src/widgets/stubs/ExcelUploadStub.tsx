import { WidgetStub } from "./WidgetStub";

export function ExcelUploadStub() {
  return (
    <WidgetStub title="엑셀 업로드" label="예정">
      전체 설정 &gt; 엑셀 가져오기에서 매핑과 샘플 검증 UI를 확인하세요.
      실제 파일 파싱과 적용은 아직 연결하지 않았습니다.
    </WidgetStub>
  );
}
